<div class="book-page">
    <a href="/book">Voltar</a>
    <div class="container">
        {{#with book}}
        <h1>{{title}}</h1>
        <p>Este livro tem {{page_qty}} páginas.</p>
        <div class="actions">
            <a href="/book/edit/{{id}}">Editar</a>
            <form action="/api/book/delete/{{id}}" method="post"><input type="submit" value="Excluir"></form>
        </div>
        {{/with}}
    </div>
</div>